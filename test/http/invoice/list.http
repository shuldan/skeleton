### ═══════════════════════════════════════════════════════
### Сценарий Command Bus: task complete → invoice created
### ═══════════════════════════════════════════════════════

### 1. Список счетов (пустой)
GET {{BASE_URL}}/api/v1/invoices
Accept: application/json

> {%
    client.test("Ответ 200", function () {
        client.assert(response.status === 200, "Expected 200");
    });
%}
