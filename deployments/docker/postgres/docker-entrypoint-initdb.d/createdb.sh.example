#!/bin/bash
#
# Скопируйте этот файл в createdb.sh и раскомментируйте нужные блоки.
# Скрипт выполняется один раз при первой инициализации (когда $DATA_PATH_HOST/postgres пуст).
#
# psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
#     CREATE DATABASE skeleton_test;
#     GRANT ALL PRIVILEGES ON DATABASE skeleton_test TO $POSTGRES_USER;
# EOSQL